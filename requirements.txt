flask>=3.0,<4
flask-cors~=6.0
pandas>=2.2,<3
openpyxl~=3.1
xlrd>=2.0.1,<3
# Leitura de .xlsx ~6x mais rápida (motor "calamine" do pandas, que exige pandas>=2.2); se falhar, cai no openpyxl.
python-calamine>=0.3,<1

# Dev / CI
pytest>=9.1.1
pytest-cov
ruff>=0.6
mypy>=1.11
pre-commit>=3.8
# Testes end-to-end (Playwright) usam Node/npm, não pip: ver tests/e2e/README.md
